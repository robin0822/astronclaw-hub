# Frontend development environment.
# Do not put runtime service tokens, model keys, or tenant secrets here.

# Default business API base. Keep this aligned with backend-delivery-docs/04-frontend-api-documentation.md.
VITE_ASTRONCLAW_API_BASE_URL=/api/v1/astron-claw

# Vite dev proxy target for /api. Local development now uses the real backend service.
BACKEND_BASE_URL=http://127.0.0.1:8000
